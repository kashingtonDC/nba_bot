# Supabase project credentials.
#
# The bot uses the service_role key, which bypasses RLS. Get it from:
#   Supabase dashboard → Project Settings → API → service_role (secret)
#
# The dashboard (dashboard/index.html) uses the anon key, which is safe
# to embed publicly because RLS policies (see migrations/004_enable_rls.sql)
# restrict it to read-only access.
#
# Never commit either key. .env is gitignored.
SUPABASE_URL=https://wgdoqojxeaurrivctqwr.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR_SERVICE_ROLE_KEY_HERE
